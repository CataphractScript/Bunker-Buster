#pragma once

#include "../graph/graph.hpp"


// Initialize Graph for Scenario 1 with all cities across: IRN, IRQ, SYR, LBN, JOR, ISR
Graph load_graph_for_scenario1();

// Initialize Graph for Scenario 2 with all cities across: IRN, IRQ, SYR, LBN, JOR, ISR
Graph load_graph_for_scenario2();

// Initialize Graph for Scenario 3 with all cities across: IRN, IRQ, SYR, LBN, JOR, ISR
Graph load_graph_for_scenario3();